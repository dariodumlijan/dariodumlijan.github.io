---
import { render, type CollectionEntry } from "astro:content";

interface Props {
  job: CollectionEntry<"jobs">;
}

const { job } = Astro.props;
const { Content } = await render(job);
---

<div class="markdown job-description">
  <Content />
</div>
